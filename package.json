{
  "name": "orbit-db-cache",
  "version": "0.1.1",
  "description": "Local cache for orbit-db",
  "main": "Cache.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Haad",
  "license": "MIT",
  "dependencies": {
    "level": "^2.1.0",
    "mkdirp": "^0.5.1"
  }
}
